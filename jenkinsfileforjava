pipeline {
    agent any

    stages {
        stage('Webhook Test') {
            steps {
                echo 'GitHub Webhook Triggered Successfully'
            }
        }

        stage('Build') {
            steps {
                bat 'echo Building project...'
            }
        }
    }
}
