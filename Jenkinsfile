pipeline {
    agent any

    stages {
        stage('Webhook Test') {
            steps {
                echo 'GitHub Webhook Triggered Successfully'
            }
        }

        stage('Build') {
            steps {
                sh 'echo Building project..123.'
            }
        }
    }
}
